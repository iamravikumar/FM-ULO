@model IEnumerable<GSA.UnliquidatedObligations.Web.Models.WorkflowViewModel>



@* Will need to style a bit for multiple workflows. *@
@foreach (var workflowVM in Model)
{
    @Html.Partial("Details/Workflow/_Questions", workflowVM.QuestionsVmList)
    @Html.Partial("Details/Workflow/_Advance", new ViewDataDictionary { { "workflowId", workflowVM.Workflow.WorkflowId } })
}

